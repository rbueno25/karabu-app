# Sistema de Expedientes (Dossiers) — Plan de Implementación

> **Para Hermes:** Ejecutar paso a paso, verificando después de cada cambio.

**Goal:** Implementar lógica de expedientes como agrupador central de cotizaciones, reservas y pagos, sin romper datos existentes.

**Architecture:** Nueva tabla `dossiers` + columnas `dossier_id`/`code` en tablas existentes. Auto-creación de dossier al crear cotización/lead. Migración retroactiva para datos antiguos.

**Tech Stack:** FastAPI + SQLAlchemy async + PostgreSQL (Supabase)

---

## Tareas

### Task 1: Agregar modelo `Dossier` en `models.py`

**Archivo:** `backend/models.py`

Agregar después de `class Payment`:

```python
class Dossier(Base):
    __tablename__ = "dossiers"

    id = Column(String, primary_key=True, default=new_id)
    code = Column(String, unique=True, nullable=False, index=True)  # EXP-2026-00001
    client_id = Column(String, ForeignKey("clients.id"), nullable=False, index=True)
    status = Column(String, nullable=False, default="abierto")  # abierto, reservado, cerrado, cancelado
    created_by = Column(String, ForeignKey("users.id"), nullable=True)
    created_at = Column(DateTime(timezone=True), nullable=False, default=utcnow)
    updated_at = Column(DateTime(timezone=True), nullable=False, default=utcnow, onupdate=utcnow)

    client = relationship("Client")
    quotations = relationship("Quotation", back_populates="dossier", lazy="selectin")
    reservations = relationship("Reservation", back_populates="dossier", lazy="selectin")

    def to_dict(self):
        return {
            "id": self.id, "code": self.code,
            "client_id": self.client_id, "status": self.status,
            "created_by": self.created_by,
            "created_at": self.created_at.isoformat() if self.created_at else None,
            "updated_at": self.updated_at.isoformat() if self.updated_at else None,
        }
```

### Task 2: Agregar `dossier_id` + `code` a `Quotation` y `Reservation`

**Archivo:** `backend/models.py`

En `class Quotation`, agregar las columnas:
```python
    dossier_id = Column(String, ForeignKey("dossiers.id"), nullable=True, index=True)
    code = Column(String, nullable=True, index=True)  # COT-2026-00001
```
Y la relación:
```python
    dossier = relationship("Dossier", back_populates="quotations")
```

En `to_dict()` de Quotation, agregar:
```python
    "dossier_id": self.dossier_id, "code": self.code,
```

En `class Reservation`, agregar:
```python
    dossier_id = Column(String, ForeignKey("dossiers.id"), nullable=True, index=True)
    code = Column(String, nullable=True, index=True)  # RES-2026-00001
```
Y la relación:
```python
    dossier = relationship("Dossier", back_populates="reservations")
```

En `to_dict()` de Reservation, agregar:
```python
    "dossier_id": self.dossier_id, "code": self.code,
```

### Task 3: Función generadora de códigos secuenciales

**Archivo:** `backend/server.py`

Agregar función helper:
```python
async def _generate_code(db: AsyncSession, prefix: str) -> str:
    """Generate sequential code: COT-2026-00001, EXP-2026-00002, etc."""
    year = str(datetime.now(timezone.utc).year)
    # Count existing codes with this prefix+year pattern
    result = await db.execute(
        text(f"SELECT COUNT(*) FROM dossiers WHERE code LIKE 'EXP-{year}-%'")
    )
    dossier_count = result.scalar() or 0
    # Also count in quotations and reservations for their prefixes
    if prefix == "COT":
        result = await db.execute(
            text(f"SELECT COUNT(*) FROM quotations WHERE code LIKE 'COT-{year}-%'")
        )
    elif prefix == "RES":
        result = await db.execute(
            text(f"SELECT COUNT(*) FROM reservations WHERE code LIKE 'RES-{year}-%'")
        )
    else:
        result = await db.execute(
            text(f"SELECT COUNT(*) FROM dossiers WHERE code LIKE 'EXP-{year}-%'")
        )
    count = (result.scalar() or 0) + 1
    return f"{prefix}-{year}-{count:05d}"
```

### Task 4: Agregar `DossierIn` schema + campo `code` en `QuotationIn`

**Archivo:** `backend/server.py`

Agregar schema:
```python
class DossierIn(BaseModel):
    client_id: str
    status: str = "abierto"
```

En `QuotationIn`, agregar:
```python
    dossier_id: Optional[str] = None
    code: Optional[str] = ""
```

### Task 5: Endpoints CRUD de Dossiers

**Archivo:** `backend/server.py`

```python
@api.get("/dossiers")
async def list_dossiers(db: AsyncSession = Depends(get_db), _u=Depends(get_current_user)):
    result = await db.execute(
        select(Dossier).order_by(Dossier.created_at.desc()).limit(200)
    )
    return [d.to_dict() for d in result.scalars().all()]

@api.get("/dossiers/{did}")
async def get_dossier(did: str, db: AsyncSession = Depends(get_db), _u=Depends(get_current_user)):
    result = await db.execute(select(Dossier).where(Dossier.id == did))
    d = result.scalar_one_or_none()
    if not d:
        raise HTTPException(status_code=404, detail="Expediente no encontrado")
    # Cargar cotizaciones y reservas asociadas
    q_result = await db.execute(
        select(Quotation).where(Quotation.dossier_id == did).order_by(Quotation.created_at.desc())
    )
    r_result = await db.execute(
        select(Reservation).where(Reservation.dossier_id == did).order_by(Reservation.created_at.desc())
    )
    return {
        **d.to_dict(),
        "quotations": [q.to_dict() for q in q_result.scalars().all()],
        "reservations": [r.to_dict() for r in r_result.scalars().all()],
    }
```

### Task 6: Modificar `create_quotation` para auto-crear dossier

**Archivo:** `backend/server.py` — endpoint `POST /api/quotations` (línea ~584)

Antes de crear la `Quotation`, insertar:
```python
    # Auto-create dossier if not provided
    dossier_id = body.dossier_id
    if not dossier_id:
        dossier_code = await _generate_code(db, "EXP")
        dossier = Dossier(
            id=new_id(), code=dossier_code,
            client_id=body.client_id, status="abierto",
            created_by=u["id"],
        )
        db.add(dossier)
        await db.flush()
        dossier_id = dossier.id

    # Generate quotation code
    quote_code = await _generate_code(db, "COT")
```

Y al crear `Quotation`, agregar:
```python
    q = Quotation(
        ...
        dossier_id=dossier_id,
        code=quote_code,
    )
```

### Task 7: Modificar `create_lead` para auto-crear dossier

**Archivo:** `backend/server.py` — endpoint `POST /api/leads` (línea ~770)

Antes de crear la `Quotation` del lead, insertar:
```python
    # Auto-create dossier for the lead
    dossier_code = await _generate_code(db, "EXP")
    dossier = Dossier(
        id=new_id(), code=dossier_code,
        client_id=client.id, status="abierto",
        created_by=None,
    )
    db.add(dossier)
    await db.flush()

    # Generate quotation code
    quote_code = await _generate_code(db, "COT")
```

Y agregar `dossier_id=dossier.id, code=quote_code` al constructor de `Quotation`.

### Task 8: Modificar `convert_quotation` para vincular dossier a reserva

**Archivo:** `backend/server.py` — endpoint `POST /api/quotations/{qid}/convert` (línea ~721)

Al crear la `Reservation`:
```python
    # Generate reservation code
    res_code = await _generate_code(db, "RES")

    reservation = Reservation(
        ...
        dossier_id=q.dossier_id,
        code=res_code,
    )
```

Y actualizar el estado del dossier:
```python
    # Update dossier status to "reservado"
    if q.dossier_id:
        d_result = await db.execute(select(Dossier).where(Dossier.id == q.dossier_id))
        dossier = d_result.scalar_one_or_none()
        if dossier:
            dossier.status = "reservado"
```

### Task 9: Migración de datos existentes

Crear script `backend/migrate_dossiers.py` que:
1. Conecte a Supabase producción
2. Cree la tabla `dossiers` (si no existe)
3. Agregue columnas `dossier_id` y `code` a `quotations` y `reservations`
4. Para cada cotización sin `dossier_id`, cree un dossier y asígnelo
5. Genere códigos retroactivos (COT-2026-XXXXX, EXP-2026-XXXXX)

Ejecutar con: `uv run --with psycopg2-binary python backend/migrate_dossiers.py`

### Task 10: Deploy

```bash
cd C:\Users\bueno\Desktop\karabu-landing
git add -A
git commit -m "feat: sistema de expedientes (dossiers) + códigos secuenciales"
git push origin master
git push origin master:main
```

Esperar ~3-5 min para deploy del backend en Render.

---

## Verificación

1. `POST /api/quotations` → debe crear dossier automáticamente + código COT-2026-XXXXX
2. `POST /api/leads` → debe crear dossier automáticamente
3. `GET /api/dossiers` → debe listar expedientes
4. `POST /api/quotations/{id}/convert` → debe vincular reserva al dossier + cambiar estado
5. Cotizaciones antiguas deben tener `dossier_id` después de migración
